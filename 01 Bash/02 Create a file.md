## create an empty text file filename.txt
   `touch filename.txt`
   `cat > filename.txt

## Echo to a file
   `echo 'This replace content to a file' > data.txt`
   `echo 'This append to the end line' >> data.txt'

